<style>
    .animate-fadeIn {
        animation: fadeIn 1.2s ease-in-out forwards;
    }
</style>

<nav id="navbar" class="<%= locals.title === 'home' ? 'hide-during-splash' : '' %> bg-white z-20 fixed top-0 left-0 right-0 animate-fadeIn ">
    <!-- Desktop Navigation -->
    <div class="max-w-7xl mx-auto px-4">
        <div class="flex justify-between h-16">
            <!-- Left Section: Navigation Links -->
            <div class="hidden md:flex items-center space-x-4 pl-0">
                <a href="/" class="text-gray-700 hover:text-black transition-colors">Home</a>

                <!-- Categories Dropdown -->
                <div class="relative group">
                    <button class="flex items-center text-gray-700 hover:text-black">
                        Categories
                        <svg class="w-4 h-4 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                        </svg>
                    </button>
                    <div
                        class="absolute z-10 left-0 mt-5 w-48 rounded-[2rem] shadow-2xl bg-white ring-1 ring-black ring-opacity-5 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200">
                        <div class="py-1 mt-1">

                            <% categories.forEach((category)=> { %>

                                <a href="/user/category/<%=category.name%>" class="block ml-3 mr-3 px-4 py-2 text-sm text-gray-900 hover:bg-gray-100  hover:rounded-[0.9rem]">
                                    <%=category.name%>
                                </a>

                                <% }) %>

                        </div>

                    </div>
                </div>

                <!-- Brands Dropdown -->
                <div class="relative group">
                    <button class="flex items-center text-gray-700 hover:text-black">
                        Brands
                        <svg class="w-4 h-4 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                        </svg>
                    </button>
                    <div
                        class="absolute z-10 left-0 mt-5 w-48 rounded-[2rem] shadow-2xl bg-white ring-1 ring-black ring-opacity-5 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200">
                        <div class="py-1">
                            <a href="#" class="block ml-3 mr-3 px-4 py-2 text-sm text-gray-700 hover:bg-gray-100 hover:rounded-[0.9rem]">Brand 1</a>
                            <a href="#" class="block ml-3 mr-3 px-4 py-2 text-sm text-gray-700 hover:bg-gray-100 hover:rounded-[0.9rem]">Brand 2</a>
                            <a href="#" class="block ml-3 mr-3 px-4 py-2 text-sm text-gray-700 hover:bg-gray-100 hover:rounded-[0.9rem]">Brand 3</a>
                        </div>
                    </div>
                </div>

                <a href="/user/products" class="text-gray-700 hover:text-black transition-colors">All Products</a>
            </div>

            <!-- Center: Logo -->
            <div class="flex items-center justify-center absolute left-1/2 transform -translate-x-1/2">
                <img src="/images/audiophile-logo-black.png" alt="Brand" class="h-14 w-auto mt-1">
            </div>

            <!-- Right Section: Search, Icons -->
            <div class="hidden md:flex items-center space-x-6 ml-auto">
                        <!-- Search Bar -->
                        <div class="relative">
                            <input id="searchInput" type="text" placeholder="Search..."
                                class="w-32 focus:w-64 transition-all duration-300 px-4 py-1 text-sm text-black border border-gray-300 rounded-full focus:outline-none focus:border-gray-500">
                            <svg id="searchIcon" class="w-4 h-4 absolute right-3 top-1/2 transform -translate-y-1/2 pointer-events-none" fill="none"
                                stroke="black" viewBox="0 0 24 24 " role="button">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                            </svg>
                            <!-- Result Container -->
                            <div id="searchResult"
                                class="absolute w-64 px-4 py-2 text-sm text-black rounded-md max-h-80 overflow-y-auto  z-50 hidden flex flex-col gap-2">
                            </div>
                        </div>

                <!-- Wishlist Icon with Counter -->
                <div class="relative group">
                    <a href="/user/wishlist" class="text-gray-700 hover:text-black transition-colors p-1">
                        <i class="far fa-heart text-xl group-hover:scale-110 transition-transform duration-200"></i>
                        <% if (locals.wishlistCount !==undefined) { %>
                            <span id="wishListCountContainer"
                                class="absolute -top-2 -right-2 bg-black text-white text-xs rounded-full h-5 w-5 flex items-center justify-center transform group-hover:scale-110 transition-transform duration-200  <%= wishlistCount === 0 ? 'opacity-50' : '' %>">
                                <%= wishlistCount %>
                            </span>
                            <% } %>
                    </a>
                </div>

                <!-- Cart Icon with Counter -->
                <div class="relative group">
                    <button id="cartBtn"  class="text-gray-700 hover:text-black transition-colors p-1" <%=locals.userName?'':'disabled'%> >
                        <i
                            class="fas fa-shopping-cart text-xl group-hover:scale-110 transition-transform duration-200"></i>
                        <% if (locals.cartCount !==undefined) { %> 
                            <span id="cartCountContainer"
                                class="absolute -top-2 -right-2 bg-black text-white text-xs rounded-full h-5 w-5 flex items-center justify-center transform group-hover:scale-110 transition-transform duration-200 <%= cartCount === 0 ? 'opacity-50' : '' %>">
                                <%= cartCount %>
                            </span>
                            <% } %> 
                            
                            
                        </button>
                </div>

                <!-- User Profile with Dropdown -->
                <div class="relative group">
                    <button class="flex items-center text-gray-700 hover:text-black focus:outline-none">
                        <i class="far fa-user"></i>
                        <% if (locals.userName) { %>
                            <span class="ml-2 text-sm text-gray-700">
                                <%= userName.firstName %>
                            </span>
                            <span class="ml-1 w-2 h-2 bg-green-500 rounded-full"></span>
                            <% } else { %>
                                <span class="ml-1 w-2 h-2 bg-red-500 rounded-full"></span>
                                <% } %>
                    </button>
                    <% if (locals.userName) { %>
                        <div
                            class="absolute z-10 right-0 mt-4 w-40 rounded-2xl shadow-lg bg-white ring-1 ring-black ring-opacity-5 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200">
                            <div class="py-1">
                                <a href="/user/profile"
                                    class="block  ml-3 mr-3 px-4 py-2 text-sm text-gray-900 hover:bg-gray-100  hover:rounded-[0.9rem]">Profile</a>
                                <a href="/user/logout"
                                    onclick="sessionStorage.removeItem('splashShown')"
                                    class="block  ml-3 mr-3 px-4 py-2 text-sm text-gray-900 hover:bg-gray-100  hover:rounded-[0.9rem]">Logout</a>
                            </div>
                        </div>
                        <% }else { %>
                            <div
                            class="absolute z-10 right-0 mt-4 w-40 rounded-md shadow-lg bg-white ring-1 ring-black ring-opacity-5 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200">
                            <div class="py-1 text-center">
                                <a href="/user/login"
                                    class="block  ml-3 mr-3 px-4 py-2 text-sm text-gray-900 hover:bg-gray-100  hover:rounded-[0.9rem]">Login</a>
                            </div>
                        <% }%>
                </div>
            </div>

            <!-- Mobile Menu Button -->
            <div class="md:hidden flex items-center">
                <button
                    class="mobile-menu-button p-2 rounded-md hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-gray-200">
                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                            d="M4 6h16M4 12h16M4 18h16" />
                    </svg>
                </button>
            </div>
        </div>
    </div>

    <!-- Mobile Menu -->
    <div class="mobile-menu hidden md:hidden transition-all duration-300 ease-in-out">
        <a href="/" class="block py-2 px-4 text-sm hover:bg-gray-100">Home</a>
        <!-- Categories Submenu -->
        <div class="mobile-submenu">
            <button class="w-full flex justify-between items-center py-2 px-4 text-sm hover:bg-gray-100">
                Categories
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                </svg>
            </button>
            <div class="hidden bg-gray-50 pl-8">
                <a href="#" class="block py-2 px-4 text-sm hover:bg-gray-100">Category 1</a>
                <a href="#" class="block py-2 px-4 text-sm hover:bg-gray-100">Category 2</a>
                <a href="#" class="block py-2 px-4 text-sm hover:bg-gray-100">Category 3</a>
            </div>
        </div>
        <!-- Brands Submenu -->
        <div class="mobile-submenu">
            <button class="w-full flex justify-between items-center py-2 px-4 text-sm hover:bg-gray-100">
                Brands
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                </svg>
            </button>
            <div class="hidden bg-gray-50 pl-8">
                <a href="#" class="block py-2 px-4 text-sm hover:bg-gray-100">Brand 1</a>
                <a href="#" class="block py-2 px-4 text-sm hover:bg-gray-100">Brand 2</a>
                <a href="#" class="block py-2 px-4 text-sm hover:bg-gray-100">Brand 3</a>
            </div>
        </div>
        <a href="/new-arrivals" class="block py-2 px-4 text-sm hover:bg-gray-100">New Arrivals</a>
        <a href="/deals" class="block py-2 px-4 text-sm hover:bg-gray-100">Deals</a>
        <div class="py-2 px-4">
            <input type="text" placeholder="Search..."
                class="w-full px-4 py-1 text-sm border border-gray-300 rounded-full focus:outline-none focus:border-gray-500">
        </div>
        <!-- Mobile User Profile -->
        <% if (locals.userName) { %>
            <div class="mobile-submenu">
                <button class="w-full flex justify-between items-center py-2 px-4 text-sm hover:bg-gray-100">
                    <div class="flex items-center">
                        <i class="far fa-user mr-2"></i>
                        <span>
                            <%= userName.firstName %>
                        </span>
                    </div>
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                    </svg>
                </button>
                <div class="hidden bg-gray-50 pl-8">
                    <a href="/profile" class="block py-2 px-4 text-sm hover:bg-gray-100">Profile</a>
                    <a href="/logout" class="block py-2 px-4 text-sm hover:bg-gray-100">Logout</a>
                </div>
            </div>
            <% } else { %>
                <a href="/login" class="block py-2 px-4 text-sm hover:bg-gray-100">
                    <i class="far fa-user mr-2"></i>
                    Login
                </a>
                <% } %>
    </div>
</nav>

<!-- Add a spacer to prevent content from hiding behind the fixed navbar -->
<div class="h-16"></div>

<script>
    // Mobile menu toggle
    const mobileMenuButton = document.querySelector('.mobile-menu-button');
    const mobileMenu = document.querySelector('.mobile-menu');
    const mobileSubmenus = document.querySelectorAll('.mobile-submenu');

    mobileMenuButton.addEventListener('click', () => {
        mobileMenu.classList.toggle('hidden');
    });

    // Handle mobile submenus
    mobileSubmenus.forEach(submenu => {
        const button = submenu.querySelector('button');
        const content = submenu.querySelector('div');

        button.addEventListener('click', () => {
            content.classList.toggle('hidden');
            const svg = button.querySelector('svg');
            svg.style.transform = content.classList.contains('hidden') ? 'rotate(0deg)' : 'rotate(180deg)';
        });
    });



</script>

<script src="/js/search-header.js"></script>
<script src="https://cdn.jsdelivr.net/npm/axios@1.6.7/dist/axios.min.js"></script>
<script>
    document.getElementById('cartBtn').addEventListener('click', async () => {
      console.log('Cart Btn clicked');

      document.getElementById('loader').classList.remove('hidden')
      document.body.style.overflow = 'hidden';

    
      const productContainer = document.getElementById('productContainer');
      const cartModal = document.getElementById('cartModal');
      const cancelBtn = document.getElementById('cancelBtn');
      const subTotalEl = document.getElementById('subTotal');
      const cartCountContainer = document.getElementById('cartCountContainer')

      cancelBtn.addEventListener('click',()=>{
        document.getElementById('cartModal').classList.add('hidden')
        })

      try {
        console.log('hello');
        const response = await axios.get('/user/cart');
        console.log('response inside get cart script header',response);
        const cart = response.data.cart;
        console.log('cart',cart);
        if (!cart || !cart.items.length) {
          productContainer.innerHTML = `<p class="text-center text-gray-500">Your cart is empty.</p>`;
          console.log('HIHIH');
          document.getElementById('loader').classList.add('hidden')
          document.body.style.overflow = '';
          document.getElementById('cartModal').classList.remove('hidden')
            return
        }
        renderCart(cart);
        document.getElementById('loader').classList.add('hidden')
        document.body.style.overflow = '';
        document.getElementById('cartModal').classList.remove('hidden')
    }
        catch(error){
            console.log('error in getting cart',error);
            document.getElementById('loader').classList.add('hidden')
             document.body.style.overflow = '';
        }

        function renderCart(cart){
            console.log('inside renderCart',cart);
        productContainer.innerHTML='';

                if (!cart || cart.items.length==0) {
            productContainer.innerHTML = `<p class="text-center text-gray-500">Your cart is empty.</p>`;
            subTotalEl.textContent =  `0 ₹` 

            document.getElementById('loader').classList.add('hidden')
            document.body.style.overflow = '';
            document.getElementById('cartModal').classList.remove('hidden')
            return
        }

            cart.items.forEach((item)=>{
            console.log('item',item);
            
        
            let productDiv = document.createElement('div');
            productDiv.innerHTML=`
            <li class="flex py-6">
            <div class="size-24 shrink-0 overflow-hidden rounded-[2rem] border border-gray-200">
                <img src="${item.imgUrl}" alt="Salmon orange fabric pouch with match zipper, gray zipper pull, and adjustable hip belt." class="size-full object-cover" />
            </div>
        
            <div class="ml-4 flex flex-1 flex-col">
                <div>
                <div class="flex justify-between text-base font-medium text-gray-900">
                    <h3>
                    <a href="#">${item.name}</a>
                    </h3>
                    <p id='productPrice' class="ml-1">${item.itemTotal} ₹</p>
                </div>
                <p id='categoryName' class="mt-1 text-sm  text-gray-600">${item.category}</p>
                <p class="mt-1 text-sm text-gray-500">${item.color}</p>
                ${
                    item.appliedOffer
                    ? `<p class="text-xs text-green-600">${item.appliedOffer} offer applied</p>`
                    : ``
                }
                </div>
                <div class="flex flex-1 items-end justify-between text-sm">
                <div class="flex items-center space-x-2">
                    <p class="text-gray-500 text-md">Qty</p>
                    <form class="max-w-xs">
                        <div class="relative flex items-center">
                            <button type="button" id="decrement-button" data-input-counter-decrement="counter-input" data-product=${item.productId} data-product-color=${item.color} data-variant=${item.variantId} class="decrement shrink-0 bg-gray-100 dark:bg-gray-700 dark:hover:bg-gray-600 dark:border-gray-600 hover:bg-gray-200 inline-flex items-center justify-center border border-gray-300 rounded-md h-5 w-5 focus:ring-gray-100 dark:focus:ring-gray-700 focus:ring-2 focus:outline-none">
                                <svg class="w-2 h-2 text-gray-900 dark:text-white" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 18 2">
                                    <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M1 1h16"/>
                                </svg>
                            </button>
                            <input type="text" id="counter-input" data-input-counter class="shrink-0 text-gray-900 border-0 bg-transparent text-sm font-normal focus:outline-none focus:ring-0 max-w-[2.5rem] text-center" placeholder="" value=${item.quantity} required />
                            <button type="button" id="increment-button" data-input-counter-increment="counter-input" data-product=${item.productId} data-product-color=${item.color} data-variant=${item.variantId} class="increment shrink-0 bg-gray-100 dark:bg-gray-700 dark:hover:bg-gray-600 dark:border-gray-600 hover:bg-gray-200 inline-flex items-center justify-center border border-gray-300 rounded-md h-5 w-5 focus:ring-gray-100 dark:focus:ring-gray-700 focus:ring-2 focus:outline-none">
                                <svg class="w-2 h-2 text-gray-900 dark:text-white" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 18 18">
                                    <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 1v16M1 9h16"/>
                                </svg>
                            </button>
                        </div>
                    </form>
                </div>
            
                <div class="flex">
                <button id='remove-Button' data-variant=${item.variantId} type="button" class="remove font-medium text-indigo-600 hover:text-indigo-500">Remove</button>
                </div>
            </div>
            </div>
            </li>
            `

            productContainer.appendChild(productDiv)
            console.log('subTotalEl',subTotalEl);
            console.log(cart.total);
            subTotalEl.textContent = `${cart.total} ₹`;
        })

        }


        productContainer.addEventListener('click',async (e)=>{
            console.log('hhihihigggcfxdxddd');
        if(e.target.classList.contains('increment')){
            console.log('hi increment');
            await axios.post('/user/cart/update-quantity',{
            productId: e.target.dataset.product,
            variantId: e.target.dataset.variant,
            type: 'increment'
            })
            refreshCart();
        }

        if (e.target.classList.contains('decrement')) {
            await axios.post('/user/cart/update-quantity', {
            productId: e.target.dataset.product,
            variantId: e.target.dataset.variant,
            type: 'decrement'
            });
            refreshCart();
        }

        if (e.target.classList.contains('remove')) {
            await axios.delete(`/user/cart/${e.target.dataset.variant}`);
            refreshCart();
        }

        })

        async function refreshCart(){
        try {
            const res = await axios.get('/user/cart')
            if(res.data.cart){
            renderCart(res.data.cart);
            cartCountContainer.textContent = res.data.cartCount;
            cartCountContainer.classList.toggle(
            'opacity-50',
            res.data.cartCount === 0
        );
            }
        } catch (error) {
            console.log('Error in refreshing cart',error);
        }
        }

      })  
    </script>
    