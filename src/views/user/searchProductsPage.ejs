<script>
  tailwind.config = {
      theme: {
          extend: {
              animation: {
                  'fade-in-up': 'fadeInUp 0.6s ease-out',
                  'pulse-subtle': 'pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite',
              },
              keyframes: {
                  fadeInUp: {
                      '0%': {
                          opacity: '0',
                          transform: 'translateY(30px)'
                      },
                      '100%': {
                          opacity: '1',
                          transform: 'translateY(0)'
                      }
                  }
              },
              backdropBlur: {
                  xs: '2px',
              }
          }
      }
  }
</script>
<div class="min-h-screen bg-transparent text-white font-sans">
<!-- Main Container -->
<div class="min-h-screen flex  justify-center p-4">
  <!-- Search Box Container -->
  <div class="w-[calc(100%-2rem)] mx-auto animate-fade-in-up ">
      <!-- Search Card -->
      <div class="bg-black backdrop-blur-xl border border-white/10 rounded-3xl p-8 md:p-12 shadow-2xl hover:shadow-3xl transition-all duration-300 hover:-translate-y-1 ">
          <!-- Header -->
          <div class="text-center mb-8">
              <h1 class="text-4xl md:text-5xl lg:text-6xl font-light text-white mb-2 tracking-tight">
                  Search
              </h1>
              <p class="text-gray-400 text-lg md:text-xl font-light">
                  Find what you're looking for
              </p>
          </div>

          <!-- Search Form -->
          <form action="/search" method="GET" class="space-y-6">
              <!-- Search Input Group -->
              <div class="relative group">
                  <div class="flex flex-col md:flex-row bg-white/10 border-2 border-white/20 rounded-2xl overflow-hidden transition-all duration-300 focus-within:border-white/40 focus-within:shadow-lg focus-within:shadow-white/10">
                      <!-- Search Input -->
                      <div class="flex-1 relative">
                          <input 
                              type="text" 
                              name="query"
                              id="searchInput"
                              class="w-full px-6 py-4 md:py-5 bg-transparent border-none outline-none text-white text-lg md:text-xl placeholder-gray-400 font-light"
                              placeholder="What are you looking for?"
                              autocomplete="off"
                              required
                          >
                          <!-- Search Icon -->
                          <div class="absolute right-4 top-1/2 transform -translate-y-1/2 text-gray-400 md:hidden">
                              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
                              </svg>
                          </div>
                      </div>
                      
                      <!-- Search Button -->
                      <button 
                          type="submit" 
                          class="px-8 py-4 md:py-5 bg-gradient-to-r from-gray-700 to-gray-600 hover:from-gray-600 hover:to-gray-500 text-white font-medium text-lg transition-all duration-300 hover:shadow-lg hover:shadow-gray-500/25 active:scale-95 md:border-l md:border-white/20"
                      >
                          <span class="hidden md:inline">Search</span>
                          <svg class="w-5 h-5 md:hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
                          </svg>
                      </button>
                  </div>
              </div>

          
          </form>


      </div>
  </div>
</div>

<!-- JavaScript for Enhanced Functionality -->
<script>
  

  // Auto-focus search input on page load
  document.addEventListener('DOMContentLoaded', function() {
      const searchInput = document.getElementById('searchInput');
      searchInput.focus();
  });

  // Add loading state to form submission
  document.querySelector('form').addEventListener('submit', function(e) {
      const submitButton = this.querySelector('button[type="submit"]');
      const originalText = submitButton.innerHTML;
      
      submitButton.innerHTML = `
          <svg class="animate-spin w-5 h-5 mx-auto" fill="none" viewBox="0 0 24 24">
              <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
              <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
          </svg>
      `;
      
      submitButton.disabled = true;
      
      // Re-enable after 3 seconds (in case of slow network)
      setTimeout(() => {
          submitButton.innerHTML = originalText;
          submitButton.disabled = false;
      }, 3000);
  });
</script>
</div>