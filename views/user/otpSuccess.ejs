<style>
  body {
    font-family: 'Inter', sans-serif;
  }
  
  /* Checkmark animation */
  .checkmark {
    stroke-dasharray: 180;
    stroke-dashoffset: 180;
    animation: draw 1s ease-in-out forwards;
    stroke-width: 4;
    stroke: white;
    transform-origin: 50% 50%;
  }
  
  @keyframes draw {
    to {
      stroke-dashoffset: 0;
    }
  }
  
  /* Progress bar animation */
  .progress-bar {
    width: 0%;
    animation: progress 3s ease-in-out forwards;
  }
  
  @keyframes progress {
    to {
      width: 100%;
    }
  }
  
  /* Fade-in animation */
  .fade-in {
    opacity: 0;
    animation: fadeIn 0.8s ease-in-out forwards;
  }
  
  @keyframes fadeIn {
    to {
      opacity: 1;
    }
  }
  
  /* Delayed fade-in */
  .fade-in-delay {
    opacity: 0;
    animation: fadeIn 0.8s ease-in-out 0.5s forwards;
  }
</style>

<div class="bg-white min-h-screen flex items-center justify-center p-4">
<div class="bg-black text-white rounded-lg shadow-xl w-full max-w-md p-8 text-center">
  <div class="flex justify-center mb-6 fade-in">
    <div class="w-20 h-20 rounded-full bg-black border-2 border-gray-700 flex items-center justify-center">
      <svg class="w-12 h-12" viewBox="0 0 52 52">
        <circle class="checkmark" cx="26" cy="26" r="25" fill="none"/>
        <path class="checkmark" fill="none" d="M14.1 27.2l7.1 7.2 16.7-16.8"/>
      </svg>
    </div>
  </div>
  
  <h1 class="text-2xl font-semibold mb-6 fade-in-delay">
    You are successfully verified and redirecting to the Home Page...
  </h1>
  
  <div class="w-full bg-gray-700 h-1 rounded-full overflow-hidden mb-4 fade-in-delay">
    <div class="progress-bar h-full bg-white rounded-full"></div>
  </div>
  
  <p class="text-gray-400 text-sm fade-in-delay">
    Please wait while we redirect you
  </p>
</div>

<script>
  // Redirect after animation completes (approximately 3 seconds)
  setTimeout(() => {
    window.location.href = "/user/login"; // Redirect to 
  }, 3000);
</script>