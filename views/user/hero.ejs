<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ecommerce Hero Carousel</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        .carousel-slide {
            transition: all 0.5s ease-in-out;
            transform: scale(0.98);
        }
        .carousel-slide.active {
            opacity: 1;
            z-index: 10;
            transform: scale(1);
        }
        .carousel-slide.inactive {
            opacity: 0;
            z-index: 0;
        }
        .carousel-image {
            transition: transform 0.5s ease-in-out;
        }
        .carousel-slide:hover .carousel-image {
            transform: scale(1.03);
        }
        .carousel-container {
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
        }
    </style>
</head>
<body class="bg-white">

<!-- Hero Carousel -->
<div class="carousel-container mx-4 sm:mx-8 my-10 rounded-[2rem] sm:rounded-[5.5rem] overflow-hidden relative">
    <!-- Slides Container -->
    <div class="relative w-full h-[50vh] sm:h-[70vh]">
        <!-- Slide 1 -->
        <div class="carousel-slide active absolute inset-0 w-full h-full flex flex-col md:flex-row items-center bg-gray-100 rounded-xl">
            <img src="/images/simgot-hero.png"
                 alt="Product 1" 
                 class="carousel-image w-full h-full object-cover object-center absolute inset-0">
            <div class="absolute inset-0 bg-gradient-to-r from-black/50 md:from-black/30 to-transparent"></div>
            <div class="relative z-10 container mx-auto px-6 sm:px-12 flex flex-col md:flex-row items-center justify-start py-6 md:py-0">
                <div class="max-w-md text-center md:text-left">
                    <span class="text-xs sm:text-sm font-semibold tracking-wider text-blue-400 bg-white/90 px-3 py-1 rounded-full inline-block">NEW ARRIVAL</span>
                    <h1 class="text-2xl sm:text-3xl md:text-5xl font-bold mt-4 mb-4 leading-tight text-white">Premium Wireless Headphones</h1>
                    <p class="text-base sm:text-lg mb-6 text-gray-200">Experience crystal clear sound with noise cancellation technology.</p>
                    <div class="flex flex-col sm:flex-row gap-3 justify-center md:justify-start">
                        <a href="#" class="bg-blue-600 hover:bg-blue-700 text-white px-6 py-2 rounded-md text-center font-medium transition duration-300 shadow-lg">Shop Now</a>
                        <a href="#" class="bg-white/20 hover:bg-white/30 text-white px-6 py-2 rounded-md text-center font-medium transition duration-300 backdrop-blur-sm">Learn More</a>
                    </div>
                </div>
            </div>
        </div>

        <!-- Slide 2 -->
        <div class="carousel-slide inactive absolute inset-0 w-full h-full flex flex-col md:flex-row items-center bg-gray-100 rounded-xl">
            <img src="/images/tangzu-hero.png" 
                 alt="Product 2" 
                 class="carousel-image w-full h-full object-cover object-center absolute inset-0">
            <div class="absolute inset-0 bg-gradient-to-l from-black/50 md:from-black/30 to-transparent"></div>
            <div class="relative z-10 container mx-auto px-6 sm:px-12 flex flex-col md:flex-row items-center justify-end py-6 md:py-0">
                <div class="max-w-md text-center md:text-right">
                    <span class="text-xs sm:text-sm font-semibold tracking-wider text-pink-400 bg-white/90 px-3 py-1 rounded-full inline-block">SALE</span>
                    <h1 class="text-2xl sm:text-3xl md:text-5xl font-bold mt-4 mb-4 leading-tight text-white">Summer Collection</h1>
                    <p class="text-base sm:text-lg mb-6 text-gray-200">30% off on all wireless earbuds this season.</p>
                    <div class="flex flex-col sm:flex-row gap-3 justify-center md:justify-end">
                        <a href="#" class="bg-pink-600 hover:bg-pink-700 text-white px-6 py-2 rounded-md text-center font-medium transition duration-300 shadow-lg">Shop Sale</a>
                    </div>
                </div>
            </div>
        </div>

        <!-- Slide 3 -->
        <div class="carousel-slide inactive absolute inset-0 w-full h-full flex flex-col md:flex-row items-center bg-gray-100 rounded-xl">
            <img src="/images/audeze-hero.png"
                 alt="Product 3" 
                 class="carousel-image w-full h-full object-cover object-center absolute inset-0">
            <div class="absolute inset-0 bg-gradient-to-r from-black/50 md:from-black/40 to-transparent"></div>
            <div class="relative z-10 container mx-auto px-6 sm:px-12 flex flex-col md:flex-row items-center justify-start py-6 md:py-0">
                <div class="max-w-md text-center md:text-left">
                    <span class="text-xs sm:text-sm font-semibold tracking-wider text-amber-400 bg-white/90 px-3 py-1 rounded-full inline-block">BEST SELLER</span>
                    <h1 class="text-2xl sm:text-3xl md:text-5xl font-bold mt-4 mb-4 leading-tight text-white">Studio Speakers</h1>
                    <p class="text-base sm:text-lg mb-6 text-gray-200">Perfect sound quality for professionals.</p>
                    <div class="flex flex-col sm:flex-row gap-3 justify-center md:justify-start">
                        <a href="#" class="bg-amber-600 hover:bg-amber-700 text-white px-6 py-2 rounded-md text-center font-medium transition duration-300 shadow-lg">View Product</a>
                        <a href="#" class="bg-white/20 hover:bg-white/30 text-white px-6 py-2 rounded-md text-center font-medium transition duration-300 backdrop-blur-sm">Watch Demo</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Navigation Dots (Pill Style) -->
    <div class="absolute bottom-4 sm:bottom-6 left-0 right-0 z-20 flex justify-center gap-1">
        <button class="carousel-dot w-6 sm:w-8 h-1.5 rounded-full bg-white bg-opacity-30 hover:bg-opacity-100 transition-all duration-300 active" data-index="0"></button>
        <button class="carousel-dot w-6 sm:w-8 h-1.5 rounded-full bg-white bg-opacity-30 hover:bg-opacity-100 transition-all duration-300" data-index="1"></button>
        <button class="carousel-dot w-6 sm:w-8 h-1.5 rounded-full bg-white bg-opacity-30 hover:bg-opacity-100 transition-all duration-300" data-index="2"></button>
    </div>

    <!-- Navigation Arrows -->
    <button class="carousel-prev absolute left-2 sm:left-4 top-1/2 -translate-y-1/2 z-20 bg-white/20 hover:bg-white/30 text-white p-2 rounded-full backdrop-blur-sm transition duration-300">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 sm:h-6 w-5 sm:w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
        </svg>
    </button>
    <button class="carousel-next absolute right-2 sm:right-4 top-1/2 -translate-y-1/2 z-20 bg-white/20 hover:bg-white/30 text-white p-2 rounded-full backdrop-blur-sm transition duration-300">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 sm:h-6 w-5 sm:w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
        </svg>
    </button>
</div>

<script>
    document.addEventListener('DOMContentLoaded', function() {
        const slides = document.querySelectorAll('.carousel-slide');
        const dots = document.querySelectorAll('.carousel-dot');
        const prevBtn = document.querySelector('.carousel-prev');
        const nextBtn = document.querySelector('.carousel-next');
        
        let currentIndex = 0;
        let autoSlideInterval;
        const slideInterval = 5000; // 5 seconds
        
        function initCarousel() {
            updateCarousel();
            startAutoSlide();
            
            const carousel = document.querySelector('.carousel-container');
            carousel.addEventListener('mouseenter', pauseAutoSlide);
            carousel.addEventListener('mouseleave', startAutoSlide);
        }
        
        function updateCarousel() {
            slides.forEach((slide, index) => {
                if (index === currentIndex) {
                    slide.classList.remove('inactive');
                    slide.classList.add('active');
                } else {
                    slide.classList.remove('active');
                    slide.classList.add('inactive');
                }
            });
            
            dots.forEach((dot, index) => {
                if (index === currentIndex) {
                    dot.classList.add('bg-opacity-100', 'w-10', 'sm:w-12');
                    dot.classList.remove('bg-opacity-30', 'w-6', 'sm:w-8');
                } else {
                    dot.classList.remove('bg-opacity-100', 'w-10', 'sm:w-12');
                    dot.classList.add('bg-opacity-30', 'w-6', 'sm:w-8');
                }
            });
        }
        
        function goToSlide(index) {
            currentIndex = index;
            updateCarousel();
        }
        
        function prevSlide() {
            currentIndex = (currentIndex - 1 + slides.length) % slides.length;
            updateCarousel();
            resetAutoSlide();
        }
        
        function nextSlide() {
            currentIndex = (currentIndex + 1) % slides.length;
            updateCarousel();
            resetAutoSlide();
        }
        
        function startAutoSlide() {
            autoSlideInterval = setInterval(nextSlide, slideInterval);
        }
        
        function pauseAutoSlide() {
            clearInterval(autoSlideInterval);
        }
        
        function resetAutoSlide() {
            pauseAutoSlide();
            startAutoSlide();
        }
        
        // Event listeners
        prevBtn.addEventListener('click', prevSlide);
        nextBtn.addEventListener('click', nextSlide);
        
        dots.forEach(dot => {
            dot.addEventListener('click', () => {
                goToSlide(parseInt(dot.getAttribute('data-index')));
                resetAutoSlide();
            });
        });
        
        // Keyboard navigation
        document.addEventListener('keydown', (e) => {
            if (e.key === 'ArrowLeft') {
                prevSlide();
            } else if (e.key === 'ArrowRight') {
                nextSlide();
            }
        });
        
        // Initialize
        initCarousel();
    });
</script>

</body>
</html>